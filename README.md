# cerebrum-docs

Auto-generated from the Amos knowledge hub. **Do not edit directly** —
any changes here will be overwritten on the next sync from the source repo.

Source of truth: `knowledge-hub/projects/amos/` in the private
Amos monorepo. A doc reaches this repo only when it carries
`scope: amos/...` AND `sensitivity: public`.

To propose a change, open a PR against the source repo. The export pipeline
(`ingest/export_mintlify.py` + the `export-amos-docs` GitHub Action)
republishes on merge to `master`.

This repo is still named `cerebrum-docs` — Amos was called Cerebrum until
2026-07. The repo rename is a separate step.
